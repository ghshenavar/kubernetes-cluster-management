kube-prometheus-stack:
  crds:
    enabled: false
  grafana:
    admin:
      existingSecret: "my-app-grafana-admin"
      userKey: "admin-user"
      passwordKey: "admin-password"
    ingress:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/issuer: selfsigned-issuer
      hosts:
        - 86.38.238.174.compute.verda.run/grafana
      tls:
        - secretName: grafana-tls
          hosts:
            - 86.38.238.174.compute.verda.run/grafana
  prometheus:
    ingress:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/issuer: selfsigned-issuer
      hosts:
        - 86.38.238.174.compute.verda.run/prometheus
      tls:
        - secretName: prometheus-tls
          hosts:
            - 86.38.238.174.compute.verda.run/prometheus
  additionalPrometheusRules:
    - name: custom-alerts
      groups:
        - name: custom-alerts
          rules:
            - alert: PodRestartingTooOften
              expr: increase(kube_pod_container_status_restarts_total[10m]) > 5
              for: 2m
              labels:
                severity: warning
              annotations:
                summary: "Pod is restarting too often"
                description: "Pod {{ $labels.pod }} in namespace {{ $labels.namespace }} restarted more than 5 times in 10 minutes."
            - alert: HighNodeCPU
              expr: 100 - (avg by (instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 90
              for: 5m
              labels:
                severity: critical
              annotations:
                summary: "High CPU usage on node"
                description: "Node {{ $labels.instance }} CPU usage has been above 90% for more than 5 minutes."
  prometheus-node-exporter:
    hostNetwork: true
    hostPID: true
    tolerations:
      - operator: Exists
