replicaCount: 1

image:
  repository: nginx
  tag: "stable"

service:
  port: 80
  targetPort: 80

ingress:
  host: example.example.com
  className: nginx
  tls:
    secretName: quickstart-example-tls
  annotations: {}
    # cert-manager.io/issuer: "letsencrypt-staging"
