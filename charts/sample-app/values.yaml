replicaCount: 1

image:
  repository: docker/getting-started
  tag: latest

service:
  port: 80
  targetPort: 80

ingress:
  host: 86.38.238.174.compute.verda.run
  className: nginx
  tls:
    secretName: quickstart-example-tls
  annotations: 
    cert-manager.io/issuer: "selfsigned-issuer"
